import 'package:flutter/material.dart';

class TempOption extends StatelessWidget {
  const TempOption({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Text("temp");
  }
}
