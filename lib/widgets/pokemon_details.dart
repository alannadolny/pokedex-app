import 'package:flutter/material.dart';

class PokemonDetails extends StatelessWidget {
  const PokemonDetails({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Text("Pokemon details");
  }
}
